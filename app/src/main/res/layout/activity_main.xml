<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:fitsSystemWindows="true">
<!-- frame để tải nội dung từ fragment_home lên-->
        <FrameLayout
            android:id="@+id/frame_Layout_ListPhim"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_above="@id/bottom_navigation"
            android:background="@color/black">
        </FrameLayout>

<!--thanh menu bottom-->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:menu="@menu/menu_bottomnavigation"
        app:itemBackground="@color/color_BackGround_btnNavigation"
        android:layout_alignParentBottom="true"
        app:itemTextColor="@color/custom_color_item"
        app:itemIconTint="@color/custom_color_item"
        />
</RelativeLayout>